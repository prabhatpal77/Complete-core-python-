# tupel comperhensions generating elements into the tuple object by writing some logic in the tuple is
# known as a tuple comperhension..
# when ever we apply the tuple comperihention we will get generator object...
x=(p for p in range(10))
print(x)
